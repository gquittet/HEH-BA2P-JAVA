package strategy.ex1;

/**
 * IFormatter
 * A simple text format tool
 */
public interface IFormatter {
    
    /**
     * Format a text
     * @param text The text to format
     */
    public String toString(String text);
}